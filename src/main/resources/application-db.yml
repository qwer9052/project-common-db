


#---
server:
  port: 8080
  servlet:
    context-path: /
spring:
  config:
    activate:
      on-profile: develop

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/chat?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 9052

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
      #naming: physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      use-new-id-generator-mappings: false
    show-sql: true
    properties:
      hibernate.enable_lazy_load_no_trans: true
      hibernate.format_sql: true
    generate-ddl: true

---
server:
  port: 8080
  servlet:
    context-path: /

spring:
  config:
    activate:
      on-profile: release

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://rds-prod.cbhccc1fu4rv.ap-northeast-2.rds.amazonaws.com:3306/prod?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 9052

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
      #naming: physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      use-new-id-generator-mappings: false
    show-sql: true
    properties:
      hibernate.enable_lazy_load_no_trans: true
      hibernate.format_sql: true

#---